# Результаты ревью и проверок

Дата: 9 сентября 2026 года.

Выполнены независимое ревью, исправления и повторная проверка. Финальный ревьювер не обнаружил оставшихся P1/P2. Это результат проверки текущего кода, а не гарантия отсутствия любых дефектов.

## Исправления

- Вместо однократной генерации на первые 30 дней слоты создаются на запрошенную дату. Ошибка их создания не оставляет невосстановимое расписание без слотов.
- Идемпотентная SQL-вставка сохраняет UUID при повторных и конкурентных запросах. Конфликт создания расписания возвращает 409.
- Неверные UUID, пагинация, слишком длинный пароль и переполнение вместимости возвращают 400 вместо внутренних ошибок. Ограничен размер JSON и отклонены дополнительные JSON-значения после первого.
- Регистрация соблюдает роль из контракта и возвращает 400 при занятом email. JWT требует корректные claims, срок действия и HS256; UUID владельца нормализуется.
- `/bookings/my` включает отменённые брони будущих слотов. Порядок общей пагинации стабилен при одинаковом времени создания.
- `/_info` всегда отвечает 200; отдельный `/ready` проверяет PostgreSQL.
- Временные метки читаются из БД в UTC независимо от локального часового пояса процесса. Переполнение `DB_MAX_CONNS` не меняет значение пула случайным образом.
- Обновлены README, конфигурация golangci-lint v2 и запуск E2E в отдельном Compose-проекте.

## Выполненные проверки

| Проверка | Результат |
|---|---|
| `go test ./... -race -count=1` | Успешно |
| Тот же прогон с `ROOM_BOOKING_TEST_DATABASE_URL` | Успешно; SQL-тесты выполнены в отдельных схемах |
| `go vet ./...` | Успешно |
| `golangci-lint run ./...` | 0 замечаний |
| `golangci-lint run --build-tags e2e ./...` | 0 замечаний |
| Форматирование, `git diff --check`, Compose config, синтаксис shell | Успешно |
| `make test-e2e` | Все 4 сценария прошли на свежем PostgreSQL 16 в Docker |
| Пример curl из README | Создание комнаты, расписания, брони, просмотр и отмена прошли на отдельной свежей БД |

Покрытие составило **46,1%** в прогоне с PostgreSQL и `-coverprofile`, без `-coverpkg`. Эта цифра относится к unit- и интеграционным тестам вместе; при запуске без переменной подключения SQL-тесты пропускаются. Вызовы repository/db из тестов service при таком способе измерения не учитываются в покрытии этих пакетов.

SQL-тесты проверяют генерацию через 90 дней, чтение PostgreSQL TIME, конкурентное создание расписания и броней, стабильные UUID, повторное бронирование после отмены, фильтрацию прошлого, пагинацию и UTC при локальном часовом поясе UTC+7. HTTP E2E отдельно проверяют конкурентные запросы на дату через 60 дней и права двух зарегистрированных пользователей.

Временные Compose-проекты и их volumes удалены после проверок. Нагрузочные SLI и интеграция с внешним Conference Service не подтверждались; ограничения указаны в README.
